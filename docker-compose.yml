services:
  frontend:
    build:
      context: .
      dockerfile: infra/Dockerfile
    container_name: finance-frontend
    ports:
      - "8080:80"
    restart: unless-stopped
    environment:
      - NODE_ENV=production
    networks:
      - finance-network
networks:
  finance-network:
    driver: bridge
